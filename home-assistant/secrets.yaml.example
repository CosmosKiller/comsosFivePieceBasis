# Optional notes for Home Assistant /config/secrets.yaml
# (never commit real IPs or tokens).
#
# iotDoorIntercom: MJPEG is added via UI now (not !secret in packages).
# Keep the URL here only if other YAML still references it:
#   cosmos_door_intercom_stream: https://192.168.1.50/stream
